# Development
So, I don't know who needs this, but I'll put this here. I am very OCD with git history so if you open a PR it'll probably become incompatible with the branch within a week lol so contribute at your own risk. I'll always be grateful and try and help out getting stuff merged however I can :)
```bash
python -m venv venv
venv/bin/activate
pip install -r requirements.txt
python generate.py
```
